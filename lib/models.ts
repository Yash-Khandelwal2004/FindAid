// Import all models here so they're always registered together
// Import this file in any route that uses populate()
import "@/models/User"
import "@/models/Listing"
import "@/models/BorrowRequest"